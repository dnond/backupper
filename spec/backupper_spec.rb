require 'spec_helper'

describe Backupper do
  it 'has a version number' do
    expect(Backupper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
